public enum UIEnum
{
    None,
}

public enum ChangePageType
{
    Run,
    Push,
    Change,
}

public enum UIState
{
    Loaded,
    Inited,
    Shown,
    Hide,
}

public enum SceneLayerType
{
    View, //最底层
    Menu, //下面菜单，上面任务属性
    Window, //弹出面板
}

public enum SceneState
{
    Loaded,
    Show,
    Hide,
}